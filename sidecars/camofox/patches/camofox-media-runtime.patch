--- a/Dockerfile
+++ b/Dockerfile
@@ -36,8 +36,13 @@ RUN apt-get update && apt-get install -y \
     # Xvfb virtual display -- runs Camoufox as if on a real desktop (better anti-detection)
     xvfb \
+    # Runtime codecs used by Firefox for common H.264/AAC, VP8/VP9, AV1, MP3, Vorbis, and Opus media
+    ffmpeg \
     # Fonts
     fonts-liberation \
+    fonts-noto-core \
+    fonts-noto-cjk \
     fonts-noto-color-emoji \
+    fonts-noto-extra \
     fontconfig \
     # Utils
     ca-certificates \
